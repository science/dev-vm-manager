# Whenever spice-vdagentd starts or restarts, refresh the per-session clients.
# A package upgrade restarts the daemon but leaves clients running on the old,
# now-deleted binary, where they spin at 100% CPU forever. See
# /usr/local/sbin/spice-vdagent-guard.
[Unit]
Wants=spice-vdagent-guard.service
